'use strict'
const { User } = require('../models')

module.exports = (() => {
    class Authorization {

        static async solveCase(req, res, next) {

        }
    }

    return Authorization
})()